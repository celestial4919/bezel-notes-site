# Bezel Notes Storefront

Final Windows-first storefront for Bezel Notes.

## Paddle

Open `config.js` and replace only the placeholder value with the LIVE Paddle client-side token from Paddle > Developer tools > Authentication.

Keep the Windows price ID as:

`pri_01m31djp57fzqd3xyxg01ccscy`

Do not put a Paddle API key/secret in this repository.

## Platform behavior

- Windows: checkout enabled.
- macOS: visible as coming soon.
- Linux: visible as coming soon.

## GitHub Pages

Push the contents of this folder to the `main` branch of the separate `bezel-notes-site` repository. Enable GitHub Pages using **GitHub Actions** as the source.
